[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install --production=false"]

[phases.build]
cmds = ["bun run build", "bunx prisma generate", "bunx prisma migrate deploy"]

[start]
cmd = "bun run start"
